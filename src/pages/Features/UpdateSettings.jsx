export function UpdateSettings(props) {
    return (
        <div className="updateButtonDiv">
            <button className="updateButton">
                Update Settings
            </button>
        </div>
    );
}